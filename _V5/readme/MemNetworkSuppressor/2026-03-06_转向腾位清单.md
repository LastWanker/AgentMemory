# 2026-03-06 转向腾位清单

## 1. 已完成的“腾位”

1. `chat_app/suppressor_adapter.py` 已变成后端路由壳，不再绑定旧实现。
2. 旧逻辑收拢到 `chat_app/suppressor_backend_legacy.py`（兼容回退）。
3. 新增 `chat_app/suppressor_backend_memnet.py`（记忆网络后端入口）。
4. 新增 `agentmemory_v3/mem_network_suppressor/` runtime 壳（后续直接填真实实现）。
5. 默认后端切换为 `SUPPRESSOR_BACKEND=mem_network`。
6. `scripts/suppressor` 标记为 legacy；新增 `scripts/mem_network_suppressor` 目录和入口脚本。

## 2. 主链路冻结确认

以下未改动逻辑目标：

1. `coarse` 召回通路
2. `association` 召回通路
3. 主检索器内部算法

本次仅改 suppressor 后处理层的后端组织方式。

## 3. 后端可分离开关

1. `SUPPRESSOR_ENABLED=0`：全后处理旁路。
2. `SUPPRESSOR_BACKEND=off`：强制 no-op 后端。
3. `SUPPRESSOR_BACKEND=mem_network`：新后端入口（当前壳实现）。
4. `SUPPRESSOR_BACKEND=legacy`：旧实现兼容回退。

## 4. 旧资源保留策略

为了可回退和对照：

1. 旧 `agentmemory_v3/suppressor` 不删除，标记为 legacy。
2. 旧训练/评测脚本不删除，标记为 legacy。
3. 新开发不再向 legacy 增功能。

## 5. 下一步落地建议

1. 优先实现 `mem_network_suppressor/runtime.py` 的最小可用读写与打分。
2. 完成 mem_network artifact 规范：`manifest.json + state.json + memory_store`。
3. 建立 mem_network 专用 two-cohort 评测脚本，避免继续扩写 legacy 网格脚本。
