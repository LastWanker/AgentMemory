# 问题报告：B方案执行结果与默认链路确认（2026-02-28）

> 范围：按照 `计划书_2026-02-27_B方案默认链路切换与防踩坑改造.md` 执行一次完整链路，并记录“缓存口径、训练口径、评测口径、权重产物”是否一致。
> 备注：本次默认主看 `S-only`；`mix(auto)` 仅保留可选，不作为默认评测组。

## 1. 本次执行命令（cmd格式）

```bat
cmd /c "python -m scripts.memory_indexer.runtime_v2.doctor_data_chain --config configs/default_v2_bipartite.yaml"
cmd /c "python -m scripts.memory_indexer.runtime_v2.train_listwise_bipartite_full --config configs/default_v2_bipartite.yaml --dataset followup_plus_chat --cache-alias users"
cmd /c "python -m scripts.memory_indexer.runtime_v2.eval_followup_plus_chat_bipartite_quick --config configs/default_v2_bipartite.yaml --dataset followup_plus_chat --cache-alias users"
cmd /c "python -m scripts.memory_indexer.runtime_v2.compare_tiny_vs_bipartite --config configs/default_v2_bipartite.yaml --dataset followup_plus_chat --cache-alias users --max-eval-queries 1000 --eval-sample-mode random --eval-sample-seed 11"
```

## 2. 执行结果（按P0-P4对齐）

### 2.1 P0 数据地基与链路体检
- `doctor` 结果为 `PASS`。
- 数据规模：`memory=3728`，`eval=4806`。
- 正式缓存状态：`eval_cache_users.jsonl` 与 `memory_cache_users.jsonl` 均存在。
- 烟测缓存状态：`users_smoke` 不存在（未污染正式缓存）。
- 报告路径：`runs/rt_v2/doctor/20260228_015910/doctor.report.json`。

### 2.2 P1/P2 模型与训练协议
- 训练脚本默认链路为 `listwise + bipartite`，并以 `ranked` 负例策略训练。
- `tau` 口径已固定为常数：`0.1`（`bipartite_learnable_tau=false`）。
- `tau` 已记录到权重 meta：
  - `bipartite_tau=0.1`
  - `bipartite_learnable_tau=False`
  - `bipartite_final_tau=0.10000000149011612`
- 训练命中 query/memory 缓存后，日志明确跳过 HF/E5 初始化。

### 2.3 P3 默认入口切换
- 默认入口已切到 `runtime_v2`。
- 一键入口文档已更新为方案B默认链路：`README/runtime_一键入口.md`。

### 2.4 P4 评测口径与结果
- 默认评测组：`S-only`。
- 本次 quick eval（1000 随机样本）结果：
  - `Recall@k=0.409`
  - `Recall@P=0.386`
  - `CoarseRankRecall@k=0.402`
  - `CoarseRankRecall@P=0.387`
  - `Gain@k=0.007`
  - `Gain@P=-0.001`
  - `MRR=0.670`
  - `Top1=0.616`
- 评测 run 路径：`runs/rt_v2/followup_plus_chat_eval/20260228_030426/bipartite/eval/`。

## 3. 对照证据（tiny vs bipartite，S-only）

- tiny（`listwise_reranker.pt`）：
  - `Recall@k=0.394`
  - `Recall@P=0.370`
  - `Gain@k=-0.008`
  - `Gain@P=-0.016`
- bipartite（`listwise_bipartite_reranker.pt`）：
  - `Recall@k=0.409`
  - `Recall@P=0.386`
  - `Gain@k=0.007`
  - `Gain@P=-0.001`
- 结论：在同口径 `S-only` 下，bipartite 相对 tiny 有一致性提升，且 `Gain@k` 已转正；`Gain@P` 接近0但仍略负，需要后续继续优化。
- 对照 run 路径：`runs/rt_v2/compare_tiny_vs_bipartite/20260228_030655/`。

## 4. 本次产物确认

- 覆盖后的默认权重：
  - `data/ModelWeights/listwise_bipartite_reranker.pt`
- 对应训练来源 run：
  - `runs/rt_v2/listwise_bipartite_train/20260228_023027/train/`
- 训练日志：
  - `runs/rt_v2/listwise_bipartite_train/20260228_023027/train/train.log.txt`
- 训练指标：
  - `runs/rt_v2/listwise_bipartite_train/20260228_023027/train/train.metrics.json`

## 5. 关键确认（针对历史问题）

1. 已确认：无签名模式下不再做“缓存条数必须等于请求条数”的强绑定；评测按缓存存在即复用，再随机采样1000。
2. 已确认：缓存命中时训练/评测都可跳过 HF/E5 初始化，避免无必要加载模型。
3. 已确认：默认评测主口径是 `S-only`，`mix(auto)` 改为可选项。
4. 已确认：`tau` 已采用固定常数策略并透出到权重 meta 与日志，口径一致。

## 6. 仍需关注的风险

- bipartite 的 `scorer/query_ms` 明显高于 tiny（本次约 `85ms` vs `42ms`）；若后续走线上实时路径，需要单独做吞吐预算与批处理策略。
- `Gain@P` 仍未稳定转正，后续应优先从训练样本难度与loss权重继续排查，而不是先做复杂结构堆叠。

## 7. 追加排查：为何“纯coarse指标”在新旧路径看起来不同

### 7.1 结论
- `RetrievalRecall@N`、`CoarseRankRecall@k/P` 本身与 reranker 权重无关；本次差异来自 **评测采样模式不同**，不是“换模型导致粗召回变差”。

### 7.2 证据（同数据指纹、同cache_alias、同candidate_mode）
- 三个 run 的 `data_fingerprint` 都是 `mem_14f739aa__eval_79448637`，`cache_alias=users`，`candidate_mode=coarse`，`top_n=30`。
- 差异点在 `eval_sample_mode`：
  - `runs/rt/followup_plus_chat_pipeline/20260227_230144/listwise/eval/eval.log.txt`：`mode=head`
  - `runs/rt/followup_plus_chat_pipeline/20260228_034917/listwise/eval/eval.log.txt`：`mode=head`
  - `runs/rt_v2/followup_plus_chat_eval/20260228_030426/bipartite/eval/eval.log.txt`：`mode=random`

### 7.3 复现实验（已执行）
- 在 v2 下强制改为 `head`（其余参数不变）：
```bat
cmd /c "python -m scripts.memory_indexer.runtime_v2.eval_followup_plus_chat_bipartite_quick --config configs/default_v2_bipartite.yaml --dataset followup_plus_chat --cache-alias users --eval-sample-mode head --eval-sample-seed 11"
```
- 结果与 legacy 高值一致：
  - `RetrievalRecall@N=0.609`
  - `CoarseRankRecall@k=0.530`
  - `Gain@k=0.020`
  - `Gain@P=0.002`
- run 路径：`runs/rt_v2/followup_plus_chat_eval/20260228_040312/bipartite/eval/`

### 7.4 具体“哪里不一样”
- legacy quick 脚本默认是 `head`：`scripts/memory_indexer/runtime/eval_followup_plus_chat_quick.py`
- v2 quick 脚本默认是 `random`：`scripts/memory_indexer/runtime_v2/eval_followup_plus_chat_bipartite_quick.py`
- 所以你看到的“v2/老路径 coarse 指标不一致”，本质是 **评测query子集不一致**。
